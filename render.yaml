services:
  # Backend Service
  - type: web
    name: baustellen-backend
    env: node
    rootDirectory: backend
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
